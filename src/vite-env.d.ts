/// <reference types="vite/client" />

/*
 * Vite's ambient types: `import.meta.glob`, the `?raw` import suffix, and asset
 * imports. Referenced rather than added to `tsconfig.json`'s `types` array so
 * the dependency is visible where it is used.
 */
